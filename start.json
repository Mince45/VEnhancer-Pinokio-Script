module.exports = async (kernel) => {
  return {
    daemon: true,
    run: [
      {
        method: "shell.run",
        params: {
          venv: "env",
          message: "python main.py --input {{input}} --output {{output}} --scale 2 --fps 60",
          path: "app"
        }
      }
    ]
  }
}