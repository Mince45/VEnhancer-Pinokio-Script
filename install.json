// install.js
module.exports = {
  run: [
    // 0. Уведомление, что мы стартовали
    {
      method: "notify",
      params: {
        html: "🚀 Начинаем установку VEnhancer..."
      }
    },
    // 1. Клонируем в папку app
    {
      method: "shell.run",
      params: {
        message: "git clone https://github.com/Vchitect/VEnhancer.git app"
      }
    },
    // 2. Создаем виртуальное окружение
    {
      method: "shell.run",
      params: {
        message: "python -m venv app/env"
      }
    },
    // 3. Устанавливаем зависимости, используя venv
    {
      method: "shell.run",
      params: {
        venv: "app/env",
        message: "pip install -r app/requirements.txt"
      }
    },
    // 4. Финальное уведомление
    {
      method: "notify",
      params: {
        html: "✅ VEnhancer установлен. Перейдите на вкладку «Start»."
      }
    }
  ]
}
